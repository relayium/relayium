<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only. `10.0.2.2` is the emulator's route to the host loopback,
     and `localhost`/`127.0.0.1` cover an `adb reverse` route on a real device.
     Every other destination keeps the platform default, which forbids
     cleartext, and this file does not exist in a release build at all. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
