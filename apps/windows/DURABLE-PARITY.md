# Windows desktop client

The Windows client is in development toward parity with released macOS 1.3.10.
It is intended for direct distribution as an EXE outside the Microsoft Store.
It is **not feature complete, not signed, and has no public release.**

## Current status

The Windows lane is green at integration `321d131be`
([run 34582006339](https://github.com/relayium/relayium/actions/runs/34582006339)):
all three jobs, `npm run check`, the Go helper suites, **2274 unit tests with 125
skips across 109 files**, `npm run build`, the Electron bootstrap and resident
lifecycle smokes, the secret durability matrix, an unsigned packaged installer,
installed-artifact acceptance, the native update runtime, the owned-process
guardian and the realtime pairing acceptance.

Superseded status note, corrected 2026-09-11: this section previously pinned
current status at `1753c3192`/`98af8a235` and described three areas as owed that
have since been delivered and proven on Windows — Account host wiring, the
Device Inbox send/named-history/recovery batch, and the update interface. The
Stored copy-history race it recorded as under repair was fixed in `6a2ab6b27`.
The table below is current as of `321d131be`.

## Status by area

| Area | Proven on Windows | Owed |
|---|---|---|
| Installation and storage | Per-user x64 NSIS installer packaged and installed to a path containing a space; forced-kill relaunch, reinstall and profile continuity; junction and `subst` alias destinations refused while ordinary paths install and uninstall; scheme ownership removed on uninstall with a foreign association preserved | Signing, distribution and upgrade acceptance. The uninstall policy is settled and asserted rather than owed: the installer's SECOND refusal is exercised too — a network destination is refused by its spelling, before any filesystem call, with the error level that says which refusal happened rather than merely non-zero, so the guard keeping private data off an SMB share is no longer only a comment. The uninstaller removes what it registered — the scheme, both send verbs, the SendTo entry — and the installed-artifact acceptance proves on a real machine that the private data root and the sealed identity SURVIVE it, byte-for-byte, and that a `relayium://` association pointing at another program is left alone. Keeping user data across an uninstall is what macOS does too |
| Secrets | Direct DPAPI writes with read-only migration from legacy `safeStorage`; a 7-cell durability matrix across forced kills; the helper's tamper oracle over 310 positions — **620 single-byte mutations: 588 refused with no output, 32 opened byte-for-byte unchanged, 0 altered** — plus refusal of DPAPI-valid blobs whose inner record does not verify. **The cross-user boundary is now PROVEN rather than declared unproven**: CI creates a second local account, that account seals its own blob successfully — which is what makes the rest mean anything, since an account with no DPAPI master key would fail to open ANYTHING — and it then cannot open this account's blob, exit 3. Run 34674997632 | — |
| Realtime transfer | The real Windows client and a real browser in one room over a real server: both link roles and both code roles, an agreed verification code, UTF-8 both ways, and byte-exact nested Unicode, empty and chunk-boundary files landing on the **real Windows destination** through the real lease and path guards. A Windows-illegal name is refused for that reason, nothing is written, and the interface reports it. Both browser and Electron process trees proven empty afterwards by job-object census | Real-network transfer; NAT and TURN remain undemonstrated. **Observed intermittent, 2026-09-12:** in hosted run 34655707714 the pairing acceptance's FIRST round timed out after 90s waiting for the link pane while rounds 2 and 3 established in 1-2s; a re-run of the same commit passed all rounds. Twelve consecutive earlier runs that night were green. **Diagnosed and addressed, same day:** the round had already paired — `browserRole=initiator` is only set after both peers are seen — and then hung because the browser asked for the workspace ONCE, best-effort, with the failure swallowed. Only an initiator round needs that ask, which is why rounds 2 and 3 were unaffected. The ask is now retried within the budget and a failure names its cause; proved on a throwaway branch by breaking the selector, where the initiator round reported the control's absence with the page text and both responder rounds stayed silent. The retry is reasoned from the role correlation rather than from a reproduction: the flake was seen once in thirteen runs |
| Stored links | Receive backend, manifest validation, native destination, cancellation, retained cleanup and the upload engine. Stored **send and history run in the resident client on Windows**, from `77c` through `1753` | Narrowed, and the row previously undersold it: the shared upload engine, its transport, its fence and its planner ARE driven against a real Go server on Windows, in `inbox-server-acceptance.mjs`, because the Device Inbox delivers through the same machinery. The stored-LINK lifecycle now runs there too: an object publishes, the SHIPPING receiver opens a `relayium://d/<id>#k=…` link minted in the same run, the plaintext returns byte for byte, and a link carrying a different key fails on `integrity` rather than being refused earlier. The copy-history race is fixed in `6a2ab6b27`. **Saving a burn-after-read link used it up silently.** The server deletes the object after one successful GET, so the link stops working for everybody — including the person who just used it — and Windows said nothing. macOS warns on its facts screen (`download.burnNotice`); Windows has no facts screen, and the code knew it: the comment at the destination picker reads "Windows has no facts surface before it yet, so the number belongs here rather than nowhere", while `facts.burnAfterRead` sat unread in the same object. The picker is where the write is authorised and the last moment anyone could be told, so that is where it is told — as a second complete title rather than a second template variable, because `CountedMessageKey` exists to hold the main process's interpolation at exactly one substitution. It says the LINK is used up, not that files are deleted: different claims, and only the first is true. **Two of this app's own pins caught the change and both were right.** The dead-copy pin refused the new key because the chooser was living inside the catalogue file, which that pin excludes from its own haystack — a key referenced only where it is defined has no reader; the chooser moved out, which is also where it belongs, since its call site is the branch composition replaces with a test seam and the choice was otherwise unexecutable. And `l10n.test.ts` reported the new key as an unexpected template because its list of counted keys was a hand-written copy of one member — the FOURTH restated set found this way today, now derived from `COUNTED_MESSAGE_KEYS`, with both directions proved. Six injected defects | Interface polish, and no stored link has been opened by a person on Windows. The burn notice itself has been read by nobody: it renders in a native dialog, which no suite opens |
| Account | Native device-code sign-in, cancellation, expiry and installation identity; the bearer never leaves main. **13 modules and their 109 owning tests executed on Windows**. Host wiring delivered in `6a2ab6b27` and green on Windows since; the account screen and device management are reachable in the shipping shell | Rename and revoke now run against a REAL account on Windows through the shipping client and normaliser: the list is re-read after each mutation, an astral-character name round-trips through central, and a repeat revoke is idempotent and resurrects nothing. Plan and usage figures are still read-only and unasserted against a real plan change. **A gap found by auditing macOS's own string catalogue and now closed:** the screen STATED "Email not verified" — a badge whose own comment says it is the reason several server actions will refuse later — and offered nothing to do about it, so the only way out was to find the original email or leave for a browser. macOS has had `content.resendVerification` since its account screen shipped and the Web client has the same affordance. The button now sits beside the badge and is absent where it could do nothing. The address is never a parameter anywhere on the path: the channel carries no payload, and `AccountClient` reads the address from the profile the server returns for the credential main just captured — a channel that took an address would let the page make this app email anybody, and the loopback case asserts the exact bytes on the wire. `POST /api/auth/email/resend` answers 200 in every case, by design, so the only honest claim is that it was ASKED for; macOS's three sentences are reused word for word in both languages rather than re-written, and a reply lost on the way back reports `requested` rather than a failure, because the POST left and the sentence — check your spam folder — stays true. A fresh profile read makes "already verified" a real outcome rather than a confusing failure when somebody finishes verifying in a browser mid-screen, and it re-READS the profile so the badge stops contradicting the sentence beside it. Proved at three layers with fifteen injected defects: the client against a real loopback server, the facade's admission discipline, the controller's supersession, and the real Electron renderer in both languages. **A platform difference, not a gap:** macOS carries 47 `subscription.*` keys because an App Store build must sell through StoreKit; Windows has no in-app purchase and routes billing to the pinned origin's account page through `accountManage`, which is a closed token main resolves. Do not read the Account row as byte-for-byte parity with macOS's account surface. **Still unproven by a person:** nobody has pressed the resend button on Windows or received the email |
| Device Inbox | Backend, encrypted journal, vault and key storage, account lifecycle, the Windows native-destination acceptance suite, a 39-case real-Go server run, the manual receive and policy slices, and the `98af8a235` send, named-history and recovery batch, which has run on Windows in every lane since | Delivery to a second real device across a network The retained-cleanup card stopped explaining itself with an OS errno: it rendered `{handle.reason}`, which `receiver.ts` fills from `codeOf(error)` — `error.code` verbatim — so a person read `EBUSY` as the row's entire label, in any language. The row already carried `residue` (`present`/`none`/`unknown`, bounded) and showed it to nobody; whether partly-written files are still on this PC is the only thing this card can honestly say and the only thing anyone can act on, so that is what it says now, in both maintained languages through a total map. The errno stays in main, is logged at both producers where it previously reached no log at all, and no longer crosses IPC — which is what `InboxView`'s own doc already claimed. A delivery's PHASE is now the journal's own union across IPC rather than a `string`, and the page's map is total over it: the old fallback said "Needs a decision" for anything unrecognised, which does not blank a row but asserts something about somebody's files. **A failed delivery told the sender it was on its way, and that is now fixed.** `InboxPage` chose between two sentences with `state === "acked" || state === "saved"`, over the server's TEN-member task union — and `acked` is not in that union at all: it belongs to the RECEIVER's local journal (`main/inbox/journal.ts`, `TaskPhase`), a vocabulary that never crosses to the sender. So the condition was effectively `state === "saved"`, and every other state read "Delivered — waiting for that device to collect it", including `expired`, `revoked` and `failed_terminal` — three states in which central has recorded the delivery will NEVER be collected. Telling somebody their files are on the way when nothing was written is not vague, it is false, and it is the kind a person acts on. macOS has named all ten since its send screen shipped and its sentences are reused word for word in both languages. The laundering that allowed it is now gone at its source: `taskState()` cast a field ALREADY typed `TaskState` through `unknown` back to `string` and invented an eleventh value, `"unknown"`, for anything else — while `SendTransport.parseTask` refuses any response whose `State` is not in `TASK_STATES`, so the guarantee held all along. The vocabulary moved to the shared contract, the map is a module with a `never` guard, and `deliveryIsOver` answers terminality separately so no surface has to parse its own copy. Eleven cases, six injected defects. **The real app proves it, not just the module:** the resident smoke drives a genuine send against a real fake-central, and it caught this change the moment it landed — its assertion had been matching `/Delivered|已送达/` against the SENTENCE, which is exactly how a screen saying "Delivered — waiting…" about an expired delivery satisfied it. The page now carries `data-delivery` with the closed server state and the smoke asserts that plus the exact sentence. That correction needed two attempts, recorded because the first is instructive: asserting the copy "does not contain Delivered" passed against an injected defect that rendered the SAVED sentence, because that sentence says "Saved on the other device." A negative assertion about wording is satisfied by any wording that differs. **Accepting a delivery promised a retry that could not happen.** `acceptedText` covered six of `InboxAcceptOutcome`'s EIGHT members and three of `DeliveryReceipt`'s FOUR, and both gaps ended at "That did not work. Relayium will try again on its own." For `not-enabled` — receiving switched off on this PC — the second half is the worse half: nothing is running to try again, so a person told that waits for a delivery that is never coming. A vague catch-all wastes attention; one that makes a BEHAVIOURAL promise is checkable and false. For a `refused` receipt the fallback threw away the `InboxFailure` the receipt carries, when `BLOCKED_KEY` beside it is already total over every code that failure can hold. Both are answered by name now, `failed` is named rather than inherited so the next member cannot silently take its sentence, and the whole decision is a module with `never` guards over both unions. Seven cases, four injected defects. **Found by mechanising a lesson rather than by reading:** the previous batch noted that a file holding both the careful shape of a pattern and the loose one is a signal, so the sweep was `grep -l "satisfies Record<"` intersected with files containing untyped union chains. Two files matched; one was the batch just fixed and the other was this. `InboxPage` held THREE shapes — a total map, a `switch` with a `never`, and this chain — two careful and one not. **Not decided here (OA-037):** fourteen of the sixteen `InboxFailureCode` values reach no surface, so every failed delivery says only "failed" — including `not-enrolled` and `retention-full`, which a person could act on. Which of them macOS names is the reference, and inventing Windows-only copy would create a parity difference rather than close one. A blocked delivery names WHICH problem, in macOS's own words: its ten blocker sentences plus enrolment and key, reused rather than re-answered, with the Windows codes macOS has no twin for taking its fallback instead of a richer Windows-only vocabulary. The Web client is not a reference here and could not be — the browser never receives an Inbox delivery, so it has no receive-side journal. **Not a live defect:** `InboxFacade.state()` never returns `blocked`, so nothing reaches this surface today; the status and its reason both existed and the page threw the reason away, and this closes that before something starts producing the state. Resume now works the first time. `mayClaim` is a callback the FACADE reads inside `drain`, and `pass()` does not consult it again on the way out — so every await from the drain to the end of a pass was time with no waiter registered, and a `resumeReceiving()` landing there was DROPPED. In production that left the Resume button apparently dead for up to 30 seconds, until the next scheduled pass. A wake with nobody to hand it to is remembered now and consumed by the next nap. It had failed CI three times as a flake, and an earlier investigation the same day concluded the hazard was unreachable — wrongly, because its reproduction held the pass BEFORE the drain, where the window does not exist. |
| Updates | The update core has run on Windows, including the native signer, custody and verified-byte launch checks. The interface covering 18 states is wired into the resident host in `a654041` and its host acceptance passed | The PUBLICATION PATH now exists end to end short of the key. `web/public/apps/` held `macos/` and `android/` and nothing for Windows, so the feed URL compiled into every build was a 404; `npm run feed:stage` writes the release into that tree and VERIFIES the document with `parseManifest` and `assertSignedByPin` before a byte is written, so a feed no client could accept cannot be staged. It has no unsigned mode and it reports the pin, which is the one fact a release cannot recover later. The tooling is compiled with the app so its rules are typed and tested, and excluded from the package — asserted absent from the installed archive. The PUBLISHER half is proved against the shipping verifier: `npm run feed:manifest` writes the document `parseManifest` accepts and the detached signature `assertSignedByPin` accepts, refuses a build number that is not strictly usable, refuses an artifact host this build does not pin, and derives the key pin in the one encoding `trust.ts` takes — base64url of the raw 32 bytes, where the obvious SPKI guess is `malformed-pin` and would make every client stop checking for updates. Still owed: the real signing key, the public key compiled into the build, and an expected Authenticode publisher — all fail-closed, so no update can be delivered until they exist. No real installer has been launched |
| Desktop behaviour | Resident close/quit, tray, notifications, deep-link handling, login-item and first-run, against a real Electron process. A received name that READS as something it is not is refused at both guards: the bidi overrides and DEL were accepted by `winpath.ts` and `nameguard.go` alike, so `photo<U+202E>gnp.exe` would have been written and shown by Explorer as `photoexe.png` — an executable a reader double-clicks believing it is an image, on the one platform where the extension decides that. Refused rather than stripped, with its own reason, and tested from code points on both sides. Staged files are read through `relayium-io-helper --source-mode`, whose component-by-component walk is proven on Windows, so no ancestor can redirect the open; the refusal to cross a MOUNT POINT is now exercised too, having skipped on every previous run for want of a volume GUID path the runner could always have supplied (run 34676789857); a Windows build without the helper refuses every staged read rather than substituting Node. The installer's Explorer verbs and SendTo shortcut now reach the app: `321d131be` stages `--send-files` at launch and on `second-instance`, and the bootstrap smoke drives the real listener and asserts the pane names the file while the directory appears in neither the text nor the markup. The registered verb is now EXECUTED rather than read: the installed-artifact acceptance takes the command out of the registry, substitutes %1 as the shell does, and spawns it BOTH ways — started cold, and delivered to an app already running. That found a live defect and fixed it: the argv parser assumed the paths sat immediately after the flag, which is true of Explorer's command line and false of the one Electron reconstructs for a second instance, so right-click send worked exactly once per app lifetime. The `relayium://` association is executed the same way, both cold and against a running instance: the page is handed the link and SHOWS it, and no transfer begins by itself. That one needed no fix — `handleDeepLink` searches the whole argv rather than a position, which is precisely the difference that broke its sibling. The SendTo shortcut is executed too, which is the only BULK path: its real target and arguments are read from the `.lnk`, three real files are appended the way the shell appends them, and all three stage with all three named and no directory in the text or the markup. The staged selection is discarded first through the control a person uses, so "a held selection survives another activation" and "a discard empties it" both run on the installed build. The OS startup toggle now runs against WINDOWS rather than a stand-in: the real IPC and the shipped adapter write, and the Run key is then read with `reg` — a declined consent leaves the registry untouched, a confirmed one leaves exactly one entry naming this executable, and turning it off removes that entry. Only the modal consent is injected. A platform-guarded scenario that does not run now reports itself, so a skip cannot read as coverage | The native file picker is still supplied by injection, so it is not proven. Explorer's OWN half — that the shell performs the %1 substitution and honours MultiSelectModel — still needs the shell, and no person has right-clicked a file on Windows |
| Received files | A finished receive announces its files as per-file capability tokens on the originating document. Dragging is main's — it re-checks that the file is still the one it registered before the OS is told — and the drag image is the build's icon rather than anything derived from the file. The page is given relative paths only, and an announcement carrying an absolute path is dropped rather than rendered | **A DECIDED difference, not a gap.** A drop SENDS here, as the Choose-files button beside it does, where macOS stages a drop and waits for an explicit press. Both are gated on the same re-read intent — ticket, connection, peer identity, link generation, verification answered — so neither can land on a peer nobody dropped onto. The owner settled this on 2026-09-12 (OA-035): keep the Windows behaviour and record the difference. Revisit if a user reports sending something they meant only to stage. A drop over the file limit is now refused WHOLE on both, which is a difference that has been closed rather than recorded: the shared sender used to trim a 1500-file drop to 1000 and say nothing, on Windows and on the Web client alike. A dropped folder now arrives WHOLE or not at all, matching the rule macOS states outright: an unreadable file used to be skipped from the batch, and an errored directory page resolved to the same empty value that means end-of-directory, so a read error truncated the folder and looked like a complete read. Both callers report a refused drop rather than sending part of it. No drag has been performed by a person on Windows; the OS drag itself is only reachable through a real cursor. **Three surfaces the shared workspace has published since Windows started using it, and Windows read NONE of them.** A relay credential running out, or a lost signalling socket meaning the link can never be rebuilt, was silent on Windows — and when either ENDED the link it read as an unexplained "Failed". The web renders all three in `WorkspaceHeader.svelte` and macOS has twins for all three, so this was Windows alone. The wording taken is the WEB's, not macOS's, and for macOS's own stated reason: `link.signalingLost` says "start again from the device list" because its equivalent surface is Nearby, while Windows pairs by CODE as the web does — macOS applies the same rule to itself in `endedUnavailable` vs `endedUnavailableIOS`, where the sentence names the device it is about. A named ending REPLACES the status line rather than sitting beside it, the two warnings are warnings and gate nothing, and the terminal card offers `Start again` — `dismissLinkEnd()` alone, because also calling `disconnect()` would set `userStopped` and land the user on "Disconnected / Reconnect", an offer to rebuild the very link whose relay just expired. The decision lives in `renderer/rooms/link-ending.ts` rather than in a `{#if}` chain, which is macOS's own shape (`LinkWorkspaceCopy`) and for the reason macOS gives: Svelte has no exhaustiveness checking and nothing but a mounted renderer executes a branch. Eight cases and five injected defects, including a fourth ending added to the SHARED union to prove the `never` guard fails the build rather than rendering blank. **That gap is closed: `LinkPane` now has the seventh harness.** The realtime lane does render this screen for real, and cannot reach most of its states — no relay expires on cue, no signalling socket drops to order, no peer floods the text lane, and the publish helper hands back one reason rather than thirteen. 64 assertions now drive: both live-link warnings and the fact that neither outlives the link; a named ending REPLACING the status line rather than sitting beside it, with the two endings reading differently; Start again offered instead of Cancel, dismissing the ending and NOT also disconnecting; a plain failure being terminal with the status word back; every publish-failure reason as its own sentence with a full disk naming the disk; all six text-lane errors said out loud; and the SAS gate including its awaiting-code state. Seven injected defects. **One of them exposed a test of my own that was passing for the wrong reason:** the "a warning does not outlive its link" case set the status off `open` first, which removes the card the warnings live in — so deleting the `!terminal` guard changed nothing and every assertion stayed green. `mixed-session.svelte.ts` sets `endReason` BEFORE calling `close()`, so there is a tick where the reason is named and the status is still `open`, and that is the only place the guard can act. The case now reaches it, and the injection turns it red. **And having that harness immediately paid for itself:** a connection that could not open said "Could not connect to {peer}" over the word "Failed", while the correct sentence sat one card away, computed and stored. `mixed-text-session` catches `LinkBusyError` and `UnsupportedLinkError` and records each as its own `errorKey`; Windows has had `textPeerBusy` ("The other device is busy.") and `textUnsupported` since the text lane shipped — on the message card, which is gated on the link being OPEN. So at the one moment either was true, the right words were on a card that could not be on screen. They now replace the status word on the not-connected card, for the reason a named ending does. Only those two of the lane's six keys: the other four describe a conversation that already exists, and captioning a failed CONNECTION with "that message is too long" would be borrowing a sentence that says nothing about it. **No shared code changed** — the causes were already established and already stored, and the recorded finding that said otherwise was written from the error classes rather than the path. Four injected defects; the scenario was written failing first, against the real app. **A conversation can be cleared now, and could not be before.** `PeerWorkspace.clearText()` has been in the shared code Windows uses since the text lane shipped, the WEB renders a Clear button wired to it, and macOS has `text.clearHistory` with a confirmation — `grep -rn "clearText|clearHistory" src/renderer` returned nothing on Windows. The transcript is memory-only and the workspace deliberately outlives page navigation, so the only way to get messages off the screen was to end the link entirely: a poor trade for somebody who wants the text gone and the connection kept. It is confirmed, because an unrecoverable wipe one button from Send is a misclick away, and every sentence says the clearing is LOCAL — the other device keeps its own copy, and implying otherwise would be a claim about a machine this app cannot touch. Four injected defects, and the scenario was written failing first: no confirmation, declining clearing anyway, clearing also ending the session, and the warning losing its "local and final" wording each turn it red. **Checked in the same audit and NOT gaps:** macOS's `text.clipboardNotice` warns that copying leaves content in the system clipboard, and the Windows text lane has no copy affordance at all — the warning without the feature would be noise; and `text.noServerHistory`'s substance is already in the Windows help panel, where `helpLanWhere` says a conversation lives only on the two devices and nothing is kept on a server. **And the last recorded defect is closed with it: which side is busy.** `LinkBusyError` carried two opposite situations — eleven sites where THIS device already holds a link or refuses admission, and two where the OTHER device answered `busy` — and Windows said "The other device is busy." for both. Wrong, not vague, in the local case: it blames the peer for a link the user themselves has open. macOS keeps them apart (`endedUnavailable` vs `endedRefused`) and its comment gives the reason: the sentence names the device it is about. `side` is now a REQUIRED constructor argument with no default, so every site states which it is and a new one cannot inherit the wrong answer; the class identity is untouched, so every `instanceof` assertion in `web/` passes unchanged. `TextErrorKey` gains `selfBusy`, and the web's own `t.text[errorKey]` — an indexed lookup its catalogue type makes total — is what forced the copy to exist there rather than a promise to add it. **Two RED proofs failed to fire and both found real gaps:** collapsing the two sentences in the Windows map changed nothing, because `LinkPane` had grown its own ternary over the same union rather than using the module — the exact shape everything else in that file had replaced, now folded in as `connectionRefusedKey`; and flipping all eleven local sites to `"peer"` left every suite green, because the only test constructed the error directly and no test drove a real path to it. Three cases now do, from the manager's own behaviour, and each turns red under the matching flip. **Every partial publication failure used to render the same sentence, and for a full disk that sentence was false.** Main narrowed the `failed` path through `FAILURE_BY_CODE` and left the PARTIAL path alone: `native-helper-client.ts` checks the helper's `failed.code` only for being a non-empty string and forwards it, and those codes are the helper's own `E_*` wire vocabulary — `winio/nt_windows.go:105` classifies NTSTATUS into `E_EXISTS`, `E_ACCESS`, `E_SHARING`, `E_NO_SPACE`, `E_NOT_FOUND`, `E_NAME_TOO_LONG`, `E_TYPE_CONFLICT`, `E_DELETE_PENDING`, `E_IO`. No branch on screen could match any of them, so a full disk, a file locked by another program, a name already taken and a permission denial all said "Could not write to the folder you chose" — which for a full disk sends somebody to check permissions on a folder that is perfectly fine. `recvFailedPermission` and `recvFailedConflict` had been written for exactly those cases and had never once been reachable. `PublishFailureReason` now carries the outcomes a person can act on DIFFERENTLY — free space, close the other program, rename, choose another folder, shorten the destination — main narrows the partial path where the other map already lives, and the widening to `string` is removed at both renderer hops so a future drift is a build error rather than a catch-all. **The correction is on the record:** the first reading of this called the two unreachable branches dead copy to be deleted, which would have destroyed the right answer; reading the Go helper found the cause was a missing translation, not a missing reason. Separately, the text lane rendered NOTHING for two of its six named errors — a message that could not be sent, and a session closed because the peer sent too many, both said nothing at all — because a four-branch ternary returned `""` for the rest. Both maps are now modules with `never` guards, 21 cases and six injected defects across the two layers. **The sweep that batch earned found one more instance and closed it.** `UploadState` has four members, was widened to `string` crossing IPC, and the send-history list mapped three and ended `return state;` — so a row for an upload that never settled rendered the English identifier `pending` as its whole label, in any language. Third instance of the class this session, after the retained-cleanup card rendering `EBUSY` and the sign-in rendering `String(err)`. `pending` now has its own word in both languages and is deliberately NOT the one `ambiguous` uses: `ambiguous` may exist on the server under an id this client never learned and the row offers Check again, while `pending` was never finalized and there is nothing to check. The union moved into the shared contract as a `const` LIST, so the journal's validator set is derived from it rather than restated beside it — two places to add a member, with no way to notice adding it to only one, was the drift that let the original widening go unnoticed. Adding a fifth state now fails the build on the `never` guard AND the tests, which read the same list. **The other four `string`-typed copy maps in the renderer were checked and recorded rather than changed:** `statusText` is sound and deliberately open because Stripe's status set is the provider's; `rowNoticeOf` is total by luck rather than by construction and is written down as such; `refusalText` and `startRefusalText` are sound, the latter confirmed by reading what the inbox send service can actually put in that slot — `busy` is a log path and `still-running` is a converge reason, neither reaching the screen |
| Same network | **The screen now says who else can be on the list, and stops promising something it cannot keep.** Its lede read "Send to a device on this network, with nothing going through a server" and both halves overstated: Relayium never scans a network — it asks its rendezvous service who else arrives from the same public address — and whether a transfer runs directly or over a relay depends on the network, which this screen's OWN help panel (`helpLanBoundary`) already said, contradicting the lede above it. macOS states the real shape twice, in `nearby.safetySummary` and `nearby.explain`; Windows said it nowhere, and its help mentioned the grouping only as a reason a device you EXPECT might be missing, never as a reason one you do not expect might be present. A carrier network, a VPN or a shared gateway can put somebody else's device on that list, and the screen now says so beside the list — as a description, deliberately not as a banner, because an alarm shown on every visit is one people learn to skip. `nearby.namesDisclaimer`'s twin sits with the names it is about and interpolates the verification setting's own label, so a renamed setting cannot leave the sentence pointing at a control that is not there. What is kept from the old lede is the part that is true either way: end-to-end encryption, so Relayium cannot read it whichever path it takes. The screen NAME stays "Same network" — a colloquial name over precise body text is coherent, and renaming a nav row is a product decision, not a copy fix. Four cases, four injected defects **That gap is now closed, and it turned out to be a much bigger one.** Nothing drove this screen at all: the realtime lane pairs by CODE — its driver clicks `nav-pair` — and the bootstrap smoke only asserts `lan-start` is ABSENT when signed out. So every state here was compile-checked and never displayed. The sixth renderer harness drives them in a real Electron window: receiving off, joining, offline (asserted as a DIFFERENT screen from an empty roster, with its retry reaching the room), joined-and-empty, a two-peer roster, a peer whose hello lacks `link/1` getting a SENTENCE rather than a greyed button, a blocked peer's Connect not acting while an unblocked one does exactly once, user-stopped with the peer still present, reconnecting-after-join versus first-join, both languages, and 680px. 36 assertions, five injected defects, and the DOM order of the two new sentences is asserted so neither can drift away from the list it describes. The stand-in for `RoomController` is cast once at the mount boundary and pinned with an assignability check, so renaming a member breaks the harness rather than letting it agree with nothing — proved by renaming `everJoined`. **The first run found a real defect in the harness itself:** the click helper copied from `os-entry-main.mjs` returns `true` for a DISABLED control, so "a blocked peer's button does not act" reported success for a press that did nothing. Fixed here; `os-entry-main.mjs` still carries the weak version and that is recorded as its own item |
| On-screen help | Every browseable screen ends with six answers — purpose, the shortest path, what Relayium can see, where things end up, what goes wrong and what to do — written against Windows behaviour rather than ported, in both maintained languages. Rendered once by the shell over a total table, so a screen cannot be added without its answers. A full-row button with aria-expanded and aria-controls, closed by default, driven on all five screens in a real window. The collapsed row carries the screen's PURPOSE, as the Mac's does and for the reason the Mac gives — it is the only part always on screen — and the toggle describes what opening it gives, through a hint that had existed in both languages and been referenced by nothing. Four screens end with a link to a maintained guide and the Account screen deliberately ends with none, asserted as an absence on the real rendered DOM. The page sends a SCREEN and a language, never an address, and the control is a button so the document holds no URL to read. Every promised guide is resolved to the file that serves it in BOTH maintained languages, and the English-only Device Inbox page to its route, so a renamed slug or an untranslated guide fails the build rather than shipping a 404 | The one remaining gap is a reader's, not a build's: no guide has been opened from a Windows build by a person, because following the link leaves the app for a browser and the suites deliberately stop at the control |
| Notifications | Files saved, a message saved, something waiting, a failure, and an upload whose link is ready — each its own event, each raised from main's own observation rather than a page's claim, and each carrying nothing that is unsafe on a lock screen. Suppressed while the window is focused, except the one that exists because something is waiting, and an offer nobody has answered yet — so the interval in which a sender is waiting is no longer silent | A toast the OS REFUSES is no longer silent: Electron emits `failed` on Windows when it cannot create or show one, and nothing listened — the user saw nothing and the app believed it had told them. That path, an unsupported platform, and a throwing constructor are now reported through the same stderr channel a path or an errno goes to. **The app can raise one, and that is now observed rather than assumed.** Every other notification case here uses a fake `Notification`; they prove the logic and cannot answer whether a toast is possible at all. The resident smoke now hands the SHIPPED `showNotification` the same real dependencies `main.ts` builds, on the real runner: the platform reports support, `show()` is reached without throwing, and no `failed` arrives in the second and a half after. Run 34710681343 recorded "the OS accepted the toast". What that still cannot say — and the note says it in those words — is that anybody SAW one: nobody is watching that machine, and no notification has been seen by a person on Windows. What is no longer assumed is the IDENTITY a toast is attributed to: the AppUserModelID the process claims, the one electron-builder stamps, and the one the installed Start Menu and Desktop shortcuts actually carry are now checked to be the same string — a mismatch shows nothing and logs nothing, so every seam test would still pass |
| Notifications | **An incoming MESSAGE session announced nothing.** Windows raised one notice for an unanswered offer — "Someone wants to send you files" — from the file batch only. A conversation request (`text.status === "incomingRequest"`) fired nothing at all, and macOS has separated `notify.incomingFiles` from `notify.incomingText` since its own resident work. The justification was already written in this codebase, on the file trigger: "the one event a person has no other way to notice: nothing was clicked to start it, the window may not be in front of them, and until it is answered the sender is waiting." Every word is true of a message request — the peer sent TEXT_REQUEST, the sender sits at `waitingAccept`. It is its own notice kind rather than a second use of the file one, because that one says files, and it carries no peer, no message and no count for the reason the file one gives: it is shown where anyone standing there can read it. Two announcers rather than one, because a file batch and a conversation can both be outstanding on one room and a single memory keyed by room lets each displace the other. **Two restated sets were derived in the same pass, one of which was about to bite:** `isResidentNotice` was a chain of comparisons under a comment warning that a restated set is how a new kind gets refused at the boundary while every type checks — adding this notice would have done exactly that; and the both-languages notification case was a hand-written array of six kinds, so a seventh would have shipped with no copy and nothing saying so. Both now derive from the union. Ten cases, three injected defects | **The trigger wiring is not driven.** No suite reaches an incoming text request through the notification path: the resident smoke drives the file offer, and the realtime lane does reach `text-accept` but observes no notifications. So the announcer rule, the boundary guard and the copy are proven, and that `App.svelte` calls the right announcer in the right circumstance is not — a shared-announcer mistake was injected and no suite noticed. Same shape of gap as the two placement rows above, and unlike those it is not closed by a harness that exists |
| Tray | Opens the app and its three live surfaces, and pauses or resumes BOTH Nearby and the Device Inbox without opening the window. Pausing the Inbox stops claiming without writing the stored policy or telling central, so a mis-click in a menu costs a pause and not an enrolment; the label says what it will do and is rebuilt when the state moves. Driven against the real runtime in the resident smoke It also reports, above the actions: the account, what the Device Inbox is doing and whether Nearby is on, read from main and localized with the rest. A pause outranks the status beneath it, and an unrecognised state reports ON rather than as a fault | No tray has been driven by a person on Windows |
| Start at sign-in | **Audited against macOS's `settings.*` and found SOUND, with one correction.** macOS carries a long login-item story — approval pending, unmanaged location, refused, unconfirmed, not registered — because `SMAppService` produces those states. Windows's mechanism is different and its own set is the right one for it: four states including `disabled-by-user`, the Task Manager case that is the real analogue of being blocked in Login Items and that no checkbox on this screen can undo, and `on-by-other-means`, where something else already launches the app and a second entry would be added for one outcome. Three failure kinds, keeping "I could not find out" apart from "off" — a distinction `login-item.ts` states outright, because off is a claim about the machine and unreadable is a claim about the call. **The one defect:** the sentence chain covered six of those seven and the seventh, `consent-failed`, fell into "That could not be changed on this PC". `enable()` returns it when the CONSENT PROMPT throws, so nothing was attempted and Windows refused nothing — the reader is sent to look for a machine problem that is not there. It has its own sentence now, and both maps are total with `never` guards in a module a test can call. Inconsistency within one file was the tell: `FAILURE_KEY` twenty lines above was already a total map whose comment gives exactly this reason, and the startup sentence was the chain that rule replaced. Seven cases, four injected defects | Nobody has signed out and back in on Windows to watch a real login item fire |
| Account gating | Main pushes an authority change carrying no identity, and the shell re-reads its state from a settled phase only, so a sign-out made anywhere reaches the screen. Signed out, the Device Inbox and the send half are gated ENTIRE: each names what it needs and offers the one action that ends it, and the controls are absent rather than greyed. Anonymous link opening is deliberately ungated | Gating is proven for the two account-bound surfaces. Every `disabled` binding in the renderer WAS walked — 52 of them — and each is a busy state that says so, an empty-input state that is self-evident, a control beside an explicit refusal, or a control not rendered at all with its reason given; no dead control was found and none was manufactured A failed sign-in now says WHY in the user's language: the screen rendered `phase.message` verbatim, and what flowed in was `String(err)` — an Electron IPC rejection, wrapper text and all — or an English sentence written for a developer, so a cancelled sign-in whose cleanup failed showed `cancelled sign-in could not read back its own credential: TypeError...` to every user in every language. `app-service.ts` already stated the rule, on the neighbouring `CleanupOutcome`: a string built from an error names a path, so it goes to a log. One type in the same file did not follow it. A code crosses IPC now — `credential-remains` and `credential-uncertain` kept apart, because "a credential is on this PC" and "I could not check whether one is" are different things to tell somebody about their own machine — and the detail is logged at the fault, where it previously reached no log at all. The reason it survived is that its only test was VACUOUS: a RegExp written as a value in `toMatchObject` compares structurally, never matches, and passes for any string, so the assertion checked `kind` and nothing else. Pinned and swept. **Still unproven by a person:** no one has failed a sign-in on Windows and read the sentence. The pairing screen now gates only the half that SPENDS an account, which is what macOS does and what this row did not previously mention: `CapabilityGateView.swift` says the Cross-network screen gates "only the half that spends an account — joining a code is right beside it and needs nothing". Windows gated neither half, so pressing Create signed out spent a round trip and returned the same sentence as a refusal — copy that already existed and already said the right thing, shown reactively. It is said before the attempt now, the Create button is ABSENT rather than greyed, Join is untouched, and the sign-in beside it is deliberately not the prominent control, because a primary Sign in would outrank the one action a signed-out reader can actually take. Found by comparing macOS's view files against this app's rather than by reading this ledger — a reminder that a ledger tracks what someone already chose to track. |
| Pairing handoff | The live code is reachable as a join link and a QR built from the compiled origin. Copy names an action and carries no text, so main writes only what main retained; an unknown action is refused and the clipboard is left alone, asserted in a real Electron process | A code is now minted end to end on Windows against the real server: two mints differ, a bearer that is not real is refused BY THE SERVER, and the expiry is the 300 seconds the on-screen help promises. Scanning the QR with a phone is unproven, and no code has been redeemed by a second device |
| Presentation | Five-row navigation; English and Chinese catalogues with compile-enforced key parity; light/dark, focus and reduced-motion support | The build reports its own version from compiled metadata: two call sites read it from `npm_package_version`, which does not exist in a packaged app, so a shipped client reported a frozen literal — including the version it ENROLS with, which central validates. **Now built, and inert.** macOS has a served client-policy gate at `/api/client-policy/macos` that can refuse to run an old build; Windows had none, and a gate cannot be added to a build already in the field, so it was a launch prerequisite rather than a follow-up. The client half now exists in three parts — the model with ten invariants each carrying an adversarial case, the bounded fetch with a remembered replay barrier, and a gate that does not BUILD the product surfaces below the floor. It ships blocking nothing: the embedded floor is `0.0.0`/build `0`, no endpoint is served, and a test asserts the floor blocks nothing so raising it has to be deliberate. **All three pieces that were owed are now delivered.** The `recommended` banner is a dismissible inset in macOS's own words, above the content and never over it; the live push means a floor published now takes effect now rather than on the next start, fired only when the answer moves and shaped on arrival because it crosses IPC; and the blocked build is proved in the REAL app — every product surface absent, including the staged-selection pane that would otherwise let a build which may not run take files from Explorer. That last test corrected its own author: it first asserted the navigation rows disappear, and they do not, because `AppVersionGate` wraps the window's CONTENT and macOS's split view renders unconditionally. Windows matches. The floor VALUE remains the owner's (OA-033), and the admin control — which without a verified Windows release catalogue would let one mistyped digit refuse every installed client — is deliberately NOT built and stays behind OA-029/OA-030. | The window now carries the appearance's own background and follows a theme change: Electron's default is white, so in dark mode the app flashed white at launch and painted white bands into any area a resize exposed, under a page painting #1c1c1e. The two colours are duplicated in the main process out of necessity — a window background exists before CSS is parsed — and a test pins them to `tokens.css`. **A deliberate difference, not a gap:** Windows ships with NO spellchecker. Electron defaults it on and downloads hunspell dictionaries from the Chromium CDN on every platform except macOS, so on Windows typing would reach a Google-operated server; the Mac keeps spellcheck because its checker is local and costs no request. Off at both `webPreferences.spellcheck` and the session, the latter asserted against the running window. See DECISION-LOG. The application menu is this app's own, not Electron's default: a shipped build offered View > Reload, Force Reload and Toggle Developer Tools to every user until this batch, and macOS never did. It is replaced rather than removed, because on Windows the Edit roles are what bind Ctrl+C/V/X/A/Z — with no menu a person cannot paste into the stored-link field. Developer items remain in an engineering build and are pinned as the only difference. Five renderer suites now run on Windows against a real Electron renderer. Four of them had never run ANYWHERE — the OS-entry pane, the pairing handoff, the account screen and the update screen, 341 assertions, invoked by no workflow and no script until run 34678237172. The fifth is the Device Inbox, the screen the product is named for, which had no real-renderer coverage of any kind until run 34691182402. It now drives: the retained-cleanup card (each residue state its own sentence, none an errno, the retry releasing THIS row's key, a released row leaving, a FAILED release not pretending it did); the receipt phase line (all six journal phases, saved not failed, saved not partial, nothing reading as the removed fallback); every one of the nine `InboxStatus` members, each putting a known element with real text on screen and no two reading alike; five blocked REASONS reading differently; the failed-act notice; and the click helper's own refusal to press a disabled control. 59 assertions as of 2026-09-12 — a count that moves, so treat the list as the claim and the number as the date's. Keyboard and motion now EXECUTE in a real window — the sidebar is focusable and arrow-driven through `sendInputEvent`, navigation moves focus into the page, and reduced motion is asserted under an emulated preference — but no screen reader has been run against it, and nobody has driven it by keyboard on Windows. Display scaling is handled rather than owed: the window is fitted to the display's WORK AREA, because Windows scaling shrinks the logical screen — a 1366x768 laptop at 150% leaves 910x512, below the old fixed 880x560 floor — and Electron does not clamp this itself. Covered across the screens people actually have, plus a real window proving the platform enforces the floor it was given. What is NOT covered is how it LOOKS at a high devicePixelRatio: no scaled desktop has been seen by a person |

## What the evidence does not cover

**No network traversal is proven.** The realtime acceptance puts a real Windows
Electron client and a real Chrome in one room over a loopback server on a single
runner. That establishes the client's own behaviour on Windows — real files, real
code and link roles, the real destination and its guards — and establishes
nothing about a Windows machine reaching a Mac across a real network.

**Injected operating-system behaviour is not operating-system behaviour.** The
resident smoke drives a real Electron process, but startup registration and the
file dialog are supplied by the test.

**A backend that passes its own acceptance is not a user flow**, and a suite that
is green on the development host is not Windows evidence.

## Standing guarantees

* Stored-link fragments remain local and never enter API requests or
  diagnostics. A deep link opened by the OS necessarily arrives through process
  arguments.
* File and message content is encrypted on the network. Device Inbox ciphertext
  is uploaded under `purpose=device_task`, which is invisible in the account's
  own file list.
* Secret keys are persisted only through the protected store. A signed-out or
  disabled state does not erase local messages or recovery keys.
* A receive that cannot be completed ends visibly: the destination's own typed
  refusal stays in the privileged process, and the interface reports a terminal
  outcome with a reason.

## What is waiting on the owner

Four things stand between this client and parity, none of them engineering, and
each is queued in the workspace-only `OWNER-ACTIONS.md` with the exact action
and how it will be verified:

* **OA-029** a code-signing certificate. The READ-BACK half is built and
  proven: every run re-reads the signature from the installer it produced and
  asserts it in both directions — unsigned when no credential is configured,
  `Valid` when one is, and a failure otherwise, so an artifact can never pass as
  signed without the file itself saying so. What still needs the route decision
  is the signing ACTION and its secret wiring, because a token-bound key and a
  service-held key are not signed with interchangeably.
* **OA-030** the update publisher pin, and whether updates are commissioned at
  all. Larger than it first looked: there is no Windows publication pipeline —
  `web/public/apps/` holds `android` and `macos` and no `windows`, nothing
  produces or signs `updates.json`, and no download surface exists. The CLIENT
  half is built and strict, and `manifest.ts` already pins what a manifest must
  contain, so the generator's contract is fixed by its consumer. The pin itself
  is the subject of the certificate and does not exist until that does.
* **OA-031** a transfer across a real network, which needs two machines. Note
  that a successful one does NOT prove relay traversal: the client knows which
  relays were issued and does not surface which candidate pair was selected, so
  whether TURN carried it is a server-side observation rather than anything the
  app can report.
* **OA-032** hands-on acceptance: no Explorer right-click has started this app,
  no phone has scanned its QR, no notification has been seen on a desktop.

They were recorded here from the beginning and never queued, so the one document
the owner reads when asking "what needs me" did not mention Windows at all.

## The local gate

`npm run gates` runs all twelve — types, unit, build, and the nine smokes — and
answers with one exit code. About 107 seconds.

It exists because a written rule failed twice in one day. A batch shipped with
four `tsc` errors that `npm run check` had reported: that command is five tools
reporting in two different formats, and the output was being filtered with a
pattern that could not match `tsc`'s, so a failing run displayed
`COMPLETED 358 FILES 0 ERRORS` and read as green. The lesson was written down,
and in the next batch a ledger-editing script raised a traceback while the same
shell line went on to commit and push, so the visible output ended in a push URL
and the commit shipped incomplete.

Both were the same mistake — judging a step by the text near the end of its
output rather than by whether it succeeded — and the second happened after the
first was recorded. So the rule is a command now. The exit code is the product;
the table is for reading.

Deliberately NOT a CI step, and a test asserts the workflow does not call it:
CI runs these as separate named steps so a failure names the surface without
anybody opening a log, and collapsing them would take that away. The gate list
is checked against `package.json`, so a new smoke is missing from the local gate
until it is added — a pre-push check with a hole in it is worse than none,
because it answers "all green" about a subset nobody chose.

## Gates that remain separate

Code signing, the update feed and any public release surface are each their own
decision, and none is authorised by a green run. The same applies to the native
picker, the OS startup toggle, manual scaling behaviour and upgrade acceptance.

## Completion

Closed since this list was written: the Device Inbox send, history and recovery
batch is proven on Windows, and account wiring is accepted and reachable.

Still required for parity:


* **Reachable user flows** for files, folders, text, stored links and the Device
  Inbox. Explorer and SendTo now reach the app; the flows themselves are
  reachable in the shell, but none has been driven end to end by a person on
  Windows.
* **Update delivery completed and configured.** The interface is wired and the
  core is proven, but with no publisher pin, no expected publisher and no feed,
  every path fails closed and no update can be delivered at all.
* **Real-network transfer demonstrated.** Everything proven so far is one
  runner and a loopback server.
* **Signing, installer and upgrade acceptance**, each its own gate.
* **A supported-version gate, before the first public release.** macOS refuses
  to build its content at all when the served policy says the build is below
  minimum, so a stale binary opens no socket. Windows has no equivalent and
  there is no `/api/client-policy/windows` to read. Deferred deliberately — no
  Windows build has ever been released, so there is nothing in the field to
  protect — but it must ship in or before the first version it governs, because
  a client already out there cannot be told to stop retroactively.
* **A real Explorer right-click.** Staging is driven in the bootstrap smoke
  through the shipping `second-instance` listener with the argv the installer's
  verbs produce, but no run has started from an actual right-click on Windows.

Passing a subsystem checkpoint does not establish platform parity.
