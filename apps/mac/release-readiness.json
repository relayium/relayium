{
  "approved": false,
  "capabilities": [
    {
      "id": "realtime-files-cross-network",
      "required": true,
      "implemented": true,
      "evidence": "DirectPane + RealtimeSessionModel; browser/native E2E coverage"
    },
    {
      "id": "cloud-files",
      "required": true,
      "implemented": true,
      "evidence": "UploadPane/DownloadPane + CloudUploadModel/CloudDownloadModel tests"
    },
    {
      "id": "ephemeral-text",
      "required": true,
      "implemented": true,
      "evidence": "RealtimeTextPane + RealtimeTextSessionModel send/receive over text/1-negotiated kind-9 frames; RealtimeTextFrameTests/RealtimeTextSessionModelTests cover wire interop, consent gating, and history limits"
    },
    {
      "id": "folder-transfer",
      "required": true,
      "implemented": false,
      "evidence": "DirectPane and UploadPane explicitly reject directories"
    },
    {
      "id": "lan-nearby-devices",
      "required": true,
      "implemented": false,
      "evidence": "Outgoing only. NearbyPane + LanDiscoveryModel join the code-less room (STUN-only /api/ice, no mint, no auth, works signed out) and send files or text to a device the user picks by peer id; LanDiscoveryTests/NearbyTransferTests cover self-exclusion, no auto-select, duplicate names, stale selection and TURN exclusion. Still blocked: the app cannot RECEIVE from a nearby device — no passive listener for an unsolicited inbound offer and no responder session lifecycle/UI for one (the advanced-verification gate would apply only when that preference is on), the same missing machinery as native-receive-residency, so the far side must be a peer that listens — relayium.com today"
    },
    {
      "id": "native-receive-residency",
      "required": true,
      "implemented": false,
      "evidence": "Menu bar keeps the process alive, but no persistent incoming-transfer listener is started"
    },
    {
      "id": "account-devices-and-stored-files",
      "required": true,
      "implemented": false,
      "evidence": "AccountView shows plan/usage only; device and stored-file management remain web-only"
    },
    {
      "id": "native-drag-drop-and-finder",
      "required": true,
      "implemented": false,
      "evidence": "Cloud upload accepts file drag-in and receive can reveal in Finder; direct drag-in, folders, and drag-out are missing"
    }
  ]
}
