# Relayium native apps

- `RelayiumKit/` — pure-logic Swift package (transport, signaling, crypto, wire). Test: `cd RelayiumKit && swift test`.
- `mac/` — macOS SwiftUI app (`com.relayium.mac`), depends on the local RelayiumKit package.

Golden crypto vectors live in `RelayiumKit/Tests/Fixtures/crypto-vectors.json`,
generated by `web/scripts/gen-crypto-vectors.mjs`. Regenerate on any crypto change.

## macOS app shell

`apps/mac/Relayium.xcodeproj` is a minimal SwiftUI app (bundle id `com.relayium.mac`,
`MACOSX_DEPLOYMENT_TARGET = 13.0`) that depends on the local `RelayiumKit` package
(`../RelayiumKit`, wired as an `XCLocalSwiftPackageReference`) and renders
`Text(RelayiumKit.sodiumReady() ? "core ready" : "core FAILED")`, proving the
package links end-to-end in the app target.

Build headlessly (no signing identity required):

```
xcodebuild -project apps/mac/Relayium.xcodeproj -scheme Relayium \
  -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build
```

Launch the built app after a build:

```
open ~/Library/Developer/Xcode/DerivedData/Relayium-*/Build/Products/Debug/Relayium.app
```

(A GUI window can't be verified from a headless shell — `BUILD SUCCEEDED` plus
the `RelayiumKit` symbol being linked into the binary is the acceptance signal.)

Real Developer ID signing + notarization is a later round (R1-F); for now
`CODE_SIGN_IDENTITY = "-"` and `CODE_SIGNING_ALLOWED=NO` keep local builds ad-hoc.
