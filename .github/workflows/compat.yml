# ╔══════════════════════════════════════════════════════════════════════════╗
# ║ TEMPORARY PROBE EDIT — NEVER MERGE.                                      ║
# ║                                                                          ║
# ║ Everything in this file marked `PROBE-ONLY` belongs to the throwaway     ║
# ║ Aggregate merge-gate reusable-Ubuntu probe driven by                     ║
# ║ `.github/workflows/merge-gate-probe.yml`. It is three edits:             ║
# ║                                                                          ║
# ║   1. `on.workflow_call` was ADDED, so this lane can be invoked as a      ║
# ║      reusable workflow. That is the whole point of the probe.            ║
# ║   2. `on.pull_request` was REMOVED, so the probe PR produces exactly     ║
# ║      one run of this lane (the CALLED one) instead of two runs of the    ║
# ║      same commit that the verdict job could not tell apart.              ║
# ║   3. `concurrency.group` no longer keys on `github.workflow`, which in   ║
# ║      a CALLED run resolves to the CALLER's name and would collide with   ║
# ║      the other called lane.                                              ║
# ║                                                                          ║
# ║ `push: main` and `workflow_dispatch` behavior, the job, its command,     ║
# ║ permissions, timeout and the deliberate ABSENCE of any `paths:` filter   ║
# ║ are unchanged. Reverting is: delete the PROBE-ONLY blocks, re-add the    ║
# ║ one `pull_request:` line, and put `${{ github.workflow }}` back.         ║
# ║                                                                          ║
# ║ NOTE, expected and not a probe failure: this lane's job is the ONE       ║
# ║ required status context on `main` (`compat / wire-vectors`). With        ║
# ║ `pull_request` removed it does not run as its own check on the probe     ║
# ║ PR, so that PR cannot satisfy branch protection and cannot be merged.    ║
# ║ Edits 2 and 3 also knowingly violate                                     ║
# ║ `scripts/test/ci-event-policy-test.mjs`, so `repo-hygiene.yml` is        ║
# ║ expected to be RED. Those files are out of scope and were not            ║
# ║ touched. NEVER MERGE.                                                    ║
# ╚══════════════════════════════════════════════════════════════════════════╝
name: compat
# The always-required compatibility gate: the cheap, platform-independent checks
# that judge a CONTRACT between two implementations, and that every platform —
# present and future — has to pass before anything expensive starts.
#
# ## Why this is a workflow of its own rather than a job somewhere else
#
# It used to be the first step of `native-web-pairing.yml`, which is a 45-minute
# macOS job behind a path filter. Both of those are wrong for a contract check:
#
#   * `npm run test:vectors` needs a checkout, Node, and the PRODUCTION half of
#     `web/`'s dependency tree — `gen-crypto-vectors.mjs` imports
#     `libsodium-wrappers`. It does NOT need a macOS runner, a Go toolchain, a
#     Chrome, a Vite build, or any devDependency, and running it inside a job
#     that has all of those means the seconds-long answer waits behind minutes
#     of setup that have nothing to do with it.
#   * A path filter is per-WORKFLOW. As the first step of a filtered workflow,
#     the vector gate ran only when that workflow's own trees changed — so the
#     moment a filter is narrowed, or a NEW platform root appears with its own
#     workflow, the wire-compatibility check silently stops covering it. A gate
#     that a new platform can bypass by existing is not a gate.
#
# So this file has deliberately NO `paths:` filter. It runs on every pull
# request and every `main` push, it is cheap enough to do that, and a future
# `apps/android/**` or `apps/windows/**` cannot route around it.
#
# ## What belongs here, and what does not
#
# Belongs: a check that is seconds long, needs no platform runner, and asserts
# that two independent implementations of one wire still agree.
#
# Does not belong: anything that builds or signs a platform artifact, anything
# needing a macOS/Windows runner, and anything slow enough that always-on would
# be expensive. Those are the heavy per-platform workflows — `macos.yml`,
# `ios.yml`, `native-web-pairing.yml` — and they stay filtered.
#
# ## "Always-required" here is a property of this file, not of the repository
#
# Everything above is enforceable in workflow code and is enforced there: no
# `paths:` on either event, no job-level `if:`, no `continue-on-error`, no retry,
# a finite timeout, and a real command. That makes this gate always-RUN and
# fail-closed — it starts on every pull request and every `main` push, and it
# reports red when the contract breaks.
#
# It does not, by itself, make a red result BLOCK a merge. That is a GitHub
# branch-protection setting on `main`, it lives in repository configuration
# rather than in this repository's source, and nothing in this file can assert
# it. The check to require there is named:
#
#     compat / wire-vectors
#
# (the workflow's `name:` and the job key, joined the way GitHub renders a status
# context).
#
# THAT CONTEXT IS NOW REQUIRED ON `main`. As of 2026-08-21, after PR #6 merged as
# `24a29ec6`, `main` protection is enabled and was verified by re-reading the
# settings after the write: strict required status checks, exactly one required
# context, bound to GitHub Actions `app_id` 15368; enforce_admins false; force
# pushes and deletions disabled. The API reports the context as `wire-vectors`
# while the merge box renders `compat / wire-vectors` — a rendering difference,
# not a substituted check; the `app_id` binding is what stops a differently-owned
# check of the same job name from satisfying the rule.
#
# So this gate MAY be described as merge-required, because it is. The earlier
# instruction here — to treat it as NOT merge-required — is stale and superseded,
# as is any statement elsewhere calling it an outstanding operational
# requirement.
#
# Still NOT observed, and deliberately not claimed: a real pull request showing
# the check required in its merge box, and a merge actually blocked while it is
# red. Enforcement is verified by settings read-back only; the observation is
# tracked in `docs/CI-PLATFORM-BOUNDARY.md` and closed on the next real PR.
#
# `docs/CI-PLATFORM-BOUNDARY.md` states the whole boundary;
# `scripts/test/ci-event-policy-test.mjs` is what enforces it, because none of
# it is visible to YAML validity or to actionlint.
on:
  push:
    # Only `main`. A branch push used to start this workflow AND its pull
    # request start a second, identical run of the same commit. The
    # pull_request trigger below is what gates branch work; this one exists so
    # `main` itself is never unverified.
    branches:
      - main
    # Deliberately no `paths:` here and none under `pull_request:`. See the
    # header: a filter is exactly how a new platform root would inherit an
    # exemption from the compatibility contract without anybody deciding to
    # grant it.
  # ── PROBE-ONLY, NEVER MERGE ───────────────────────────────────────────────
  # A bare `pull_request:` used to sit here and is REMOVED for the probe.
  # Semantic being protected: the probe PR must produce ONE run of this lane —
  # the one the caller invokes — because the verdict job reads a job RESULT and
  # could not distinguish the called run from an identical
  # independently-triggered run of the same commit. This lane carries no
  # `paths:` filter on either event, so restoring the trigger is a literal
  # one-line re-add and the comment above it is untouched.
  #
  # Semantic under test: that this lane is callable at all, and what a called
  # run observes. `workflow_call` is not an event of its own — a called run
  # reports the CALLER's `event_name`/`ref` — which the probe step in
  # `wire-vectors` asserts rather than assumes.
  workflow_call:
    inputs:
      probe_assert_pull_request:
        # PROBE-ONLY. Defaults to false so the surviving `push: main` and
        # `workflow_dispatch` triggers, where the `inputs` context holds no such
        # key and this reads as null, run byte-for-byte as they did before. Only
        # a caller that passes true arms the assertion.
        description: 'PROBE-ONLY (never merge): assert the called run observes event_name=pull_request.'
        type: boolean
        required: false
        default: false
  workflow_dispatch:

# Supersede a pull request's own earlier runs, and nothing else. Identical to
# the rule in every other governed workflow, and asserted by
# `scripts/test/ci-event-policy-test.mjs`: group by PR NUMBER when there is one
# and by `github.run_id` — unique per run — otherwise, so a PR supersedes only
# itself and nothing can ever cancel a `main` or dispatch run.
#
# ── PROBE-ONLY, NEVER MERGE ─────────────────────────────────────────────────
# Semantic under test: in a CALLED workflow, `github.workflow` resolves to the
# CALLER's workflow name, not this file's. The repository-standard key is
# `${{ github.workflow }}-<pr number>`; under one caller that expands to the
# SAME string for every lane that caller invokes, so `compat` and
# `ops-deploy-contract` would share a single group and — with
# `cancel-in-progress` true on a pull request — cancel each other mid-run. The
# verdict would then read `cancelled` for a lane that was never allowed to
# finish, and the probe would be measuring its own concurrency key.
#
# The literal lane prefix below is what keeps the two called lanes in separate
# groups. It is the ONLY change: the PR-number key, the `github.run_id`
# fallback for non-PR events, and `cancel-in-progress` are untouched, so this
# lane still supersedes its OWN earlier run and still cannot cancel anything
# outside a pull request. Revert by restoring `${{ github.workflow }}`.
concurrency:
  group: probe-lane-compat-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  # No secrets, so this runs on fork pull requests like any other check.
  # Deliberately no job-level `if:`, no `continue-on-error` and no retry: a
  # compatibility gate that may skip, may be advisory, or may be re-rolled until
  # it agrees is indistinguishable from no gate, and the policy test asserts the
  # absence of all three.
  wire-vectors:
    runs-on: ubuntu-latest
    # Seconds of real work. The bound is generous only so a runner that is
    # merely slow does not read as drift, and finite so a wedged run cannot hold
    # a slot until GitHub's 6-hour default expires.
    timeout-minutes: 10
    steps:
      # ── PROBE-ONLY, NEVER MERGE — delete with the probe ──────────────────
      # Semantic under test: what a reusable workflow OBSERVES when it is
      # called. A called run does not report a `workflow_call` event; it reports
      # the CALLER's `event_name` and `ref`. This step is the evidence for that
      # claim rather than an assumption about it, so `test` — not a print — is
      # what decides: a called run that does not see `pull_request` fails the
      # lane and the verdict job reports it.
      #
      # It is placed before `checkout` because it reads nothing from the tree,
      # and it is skipped entirely on `push: main` and `workflow_dispatch`,
      # where the `inputs` context holds no such key and the `if` reads as null.
      # It changes no existing step, command, permission or bound. It does add a
      # step to a sequence `scripts/test/ci-event-policy-test.mjs` asserts by
      # position, which is part of the expected-RED `repo-hygiene.yml` result
      # described in the header and is not a probe failure.
      - name: 'PROBE (never merge): the called run observes the caller event'
        if: ${{ inputs.probe_assert_pull_request }}
        env:
          PROBE_EVENT_NAME: ${{ github.event_name }}
          PROBE_REF: ${{ github.ref }}
          PROBE_CALLER_WORKFLOW: ${{ github.workflow }}
        run: |
          printf 'called lane=compat event_name=%s ref=%s github.workflow=%s\n' \
            "$PROBE_EVENT_NAME" "$PROBE_REF" "$PROBE_CALLER_WORKFLOW"
          test "$PROBE_EVENT_NAME" = 'pull_request'
      # Third-party actions are pinned to a full commit SHA (with the
      # human-readable version in a trailing comment) so a compromised or
      # retagged upstream release can't silently change what runs here.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
      # This job DOES need a dependency install, and the comment here used to
      # say the opposite.
      #
      # It was true until `gen-crypto-vectors.mjs` joined the gate's table in
      # `5619f062`: before that, `check-wire-vectors.mjs` and both generators
      # imported nothing but `node:` builtins. That generator imports
      # `libsodium-wrappers` — a PRODUCTION dependency — so from that commit on a
      # runner with no dependency tree cannot run this gate at all. It went
      # unnoticed because a developer checkout already has `web/node_modules`
      # lying around; a clean runner gets ERR_MODULE_NOT_FOUND and the gate is
      # red for a reason that has nothing to do with the contract it checks,
      # which is the shortest path to somebody making it advisory.
      #
      # So: the smallest deterministic closure that actually runs the
      # generators, and nothing else.
      #
      #   * `npm ci`, never `npm install` — the tree comes from
      #     `package-lock.json` exactly, and the lockfile is never rewritten
      #     here. A gate that compares frozen bytes is installed from frozen
      #     bytes.
      #   * `--omit=dev` — the generators need `libsodium-wrappers`; they do not
      #     need Vite, Vitest, svelte-check or TypeScript. 31 packages rather
      #     than the whole tree.
      #   * `--ignore-scripts` — nothing in that closure needs a lifecycle
      #     script to work, and two packages in it (`yargs`, `get-caller-file`)
      #     declare `prepare` scripts shelling out to `tsc` and `npm run
      #     compile`, which `--omit=dev` has deliberately not installed.
      #
      # Deliberately still no `cache: npm`. Measured from tracked bytes only
      # with an empty npm cache, this install is ~1.2s against ~0.5s for the
      # gate itself; a setup-node cache restore-and-save round trip is not
      # reliably cheaper than that, and it would put a shared mutable artifact
      # in the path of the one check that gates every merge. A second that may
      # not exist is not worth that blast radius.
      #
      # Presence, ordering, working directory and each flag are asserted by
      # `scripts/test/ci-event-policy-test.mjs`; none of it is visible to YAML
      # validity or to actionlint.
      - name: Install the production dependency closure the generators import
        working-directory: web
        run: npm ci --ignore-scripts --omit=dev
      # `apps/RelayiumKit/Tests/Fixtures/*-wire-vectors.json` are transcriptions
      # of the Web implementation that the Swift suites assert against. If the
      # Web wire moves and the fixtures are not regenerated, every Swift vector
      # test still passes — against the OLD wire — and the two implementations
      # have diverged with a green board.
      #
      # The verifying form is observably read-only: it regenerates, compares and
      # restores what it found. The writing form (`gen:vectors`) must never
      # appear in CI, and `scripts/test/native-web-pairing-gate-test.mjs`
      # asserts that.
      - name: Regenerate the cross-language wire vectors and require zero diff
        working-directory: web
        run: npm run test:vectors
