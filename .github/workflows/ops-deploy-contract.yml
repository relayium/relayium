# ╔══════════════════════════════════════════════════════════════════════════╗
# ║ TEMPORARY PROBE EDIT — NEVER MERGE.                                      ║
# ║                                                                          ║
# ║ Everything in this file marked `PROBE-ONLY` belongs to the throwaway     ║
# ║ Aggregate merge-gate reusable-Ubuntu probe driven by                     ║
# ║ `.github/workflows/merge-gate-probe.yml`. It is three edits:             ║
# ║                                                                          ║
# ║   1. `on.workflow_call` was ADDED, so this lane can be invoked as a      ║
# ║      reusable workflow. That is the whole point of the probe.            ║
# ║   2. `on.pull_request` was REMOVED, so the probe PR produces exactly one ║
# ║      run of this lane (the CALLED one) instead of two runs of the same   ║
# ║      commit that the verdict job could not tell apart.                   ║
# ║   3. `concurrency.group` no longer keys on `github.workflow`, which in a ║
# ║      CALLED run resolves to the CALLER's name and would collide with the ║
# ║      other called lane.                                                  ║
# ║                                                                          ║
# ║ `push: main` behavior, the job, its command, permissions, timeout and    ║
# ║ path filter are unchanged, and no `workflow_dispatch` was added (this    ║
# ║ lane has never had one). Reverting is: delete the PROBE-ONLY blocks,     ║
# ║ re-add the two `pull_request:` lines, and put `${{ github.workflow }}`   ║
# ║ back.                                                                    ║
# ║                                                                          ║
# ║ NOTE, expected and not a probe failure: edits 2 and 3 knowingly violate  ║
# ║ `scripts/test/ci-event-policy-test.mjs` sections 1 and 2 and the         ║
# ║ push/pull_request filter parity check in                                 ║
# ║ `scripts/test/contract-ci-policy-test.mjs`, so `repo-hygiene.yml` is     ║
# ║ expected to be RED on the probe PR. Those files are out of scope for the ║
# ║ probe and were deliberately not touched. NEVER MERGE.                    ║
# ╚══════════════════════════════════════════════════════════════════════════╝
name: ops-deploy-contract
# The product↔ops deployment interface contract's own lane: the one command that
# judges the Go runtime half of `contracts/ops-deploy-v1.json`, and nothing else.
#
# ## Why this is not a fourth job in `contracts.yml`
#
# `contracts.yml` was filtered to `contracts/**` because at the time the tree
# held one document and every implementation of it read the same vocabulary.
# This document's readers are the Go server test and the always-on product
# policy, plus the ops deploy script once a later batch adds it. Neither of the
# other two jobs in that lane can see any of them:
#
#   * `web-contract` would `npm ci` a Vitest closure to run a test file that does
#     not exist for this contract;
#   * `swift-contract` would take a **PAID macOS runner** and a cold SwiftPM
#     resolve to run a filtered `swift test` against a document Swift never
#     opens.
#
# So the tree's ownership moved from per-TREE to per-CONTRACT-FILE.
# `contracts.yml` is now filtered to exactly the Device Inbox inputs and keeps
# its own full consumer set; this file owns this document alone, on one free Ubuntu
# runner. `scripts/test/contract-ci-policy-test.mjs` asserts both halves and, in
# place of the tree-wide filter's guarantee, requires every file under
# `contracts/` to be claimed by exactly one lane — so a third contract fails
# until somebody gives it an owner.
#
# ## Why the declarative half is not here
#
# The other consumer of this contract, `scripts/test/ops-deploy-contract-test.mjs`,
# needs no toolchain at all and runs in `repo-hygiene.yml`, which carries no path
# filter. That is deliberate and is the more important half: it re-checks every
# declared path, working directory, npm script and artifact against
# `git ls-files`, so a `web/` or `server/` change that invalidates the contract
# fails on the commit that makes it rather than on the next contract edit. Adding
# it here too would run the same check twice for one answer.
#
# ## The other direction is unchanged
#
# `server/ops_deploy_contract_test.go` lives under `server/**`, so an ordinary
# server change still starts `go.yml` and `go test ./...` still executes it. This
# lane catches a document that stopped matching the server; that suite catches a
# server that stopped matching the document. Neither substitutes for the other.
on:
  push:
    # Only `main`. A branch push would otherwise start this workflow AND its
    # pull request start a second, identical run of the same commit. The
    # pull_request trigger below is what gates branch work; this one exists so
    # `main` itself is never unverified.
    branches:
      - main
    # The document and this file. NOT `contracts/**`: the tree now holds two
    # contracts with different consumer sets, and a tree-wide filter here would
    # start this lane for a Device Inbox edit whose Go test it does not run.
    #
    # `docs/OPS-DEPLOY-CONTRACT.md` is deliberately absent for the same reason
    # `docs/DEVICE-INBOX-ADMISSION-CONTRACT.md` is absent from `contracts.yml`:
    # no job here reads it, and the always-on `repo-hygiene.yml` already fails
    # when the contract points at a document that is gone.
    #
    # `server/ops_deploy_contract_test.go` is absent too — it lives in a tree
    # `go.yml` watches, and naming it here would start two lanes for one answer.
    paths:
      - 'contracts/ops-deploy-v1.json'
      - '.github/workflows/ops-deploy-contract.yml'
  # ── PROBE-ONLY, NEVER MERGE ───────────────────────────────────────────────
  # `pull_request:` / `paths: *paths` used to sit here and is REMOVED for the
  # probe. Semantic being protected: the probe PR must produce ONE run of this
  # lane — the one the caller invokes — because the verdict job reads a job
  # RESULT and could not distinguish the called run from an identical
  # independently-triggered run of the same commit. The `&paths` anchor above is
  # deliberately left in place (an unaliased anchor is valid YAML) so restoring
  # the trigger is a literal two-line re-add and the two filters cannot drift.
  #
  # Semantic under test: that this lane is callable at all, and what a called
  # run observes. `workflow_call` is not an event of its own — a called run
  # reports the CALLER's `event_name`/`ref` — which the probe step in
  # `go-contract` asserts rather than assumes. This lane is also the one the
  # caller invokes a SECOND time behind `if: false`, to observe what a skipped
  # reusable call reports without running it.
  workflow_call:
    inputs:
      probe_assert_pull_request:
        # PROBE-ONLY. Defaults to false so the surviving `push: main` trigger,
        # where the `inputs` context is empty and this reads as null, runs
        # byte-for-byte as it did before. Only a caller that passes true arms
        # the assertion.
        description: 'PROBE-ONLY (never merge): assert the called run observes event_name=pull_request.'
        type: boolean
        required: false
        default: false

# Supersede a pull request's own earlier runs, and nothing else.
#
# The group key is the PR NUMBER, so pushing three times to a branch leaves one
# live run instead of three. Every non-PR event falls back to `github.run_id`,
# which is unique per run, so no two such runs ever share a group and nothing
# outside a pull request can cancel anything.
#
# ── PROBE-ONLY, NEVER MERGE ─────────────────────────────────────────────────
# Semantic under test: in a CALLED workflow, `github.workflow` resolves to the
# CALLER's workflow name, not this file's. The repository-standard key is
# `${{ github.workflow }}-<pr number>`; under one caller that expands to the
# SAME string for every lane that caller invokes, so `compat` and
# `ops-deploy-contract` would share a single group and — with
# `cancel-in-progress` true on a pull request — cancel each other mid-run. The
# verdict would then read `cancelled` for a lane that was never allowed to
# finish, and the probe would be measuring its own concurrency key.
#
# The literal lane prefix below is what keeps the two called lanes in separate
# groups. It is the ONLY change: the PR-number key, the `github.run_id`
# fallback for non-PR events, and `cancel-in-progress` are untouched, so this
# lane still supersedes its OWN earlier run and still cannot cancel anything
# outside a pull request. Revert by restoring `${{ github.workflow }}`.
concurrency:
  group: probe-lane-ops-deploy-contract-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# Read-only, and no job below reads a secret. This lane compares a JSON document
# to a Go HTTP handler: it publishes nothing, deploys nothing, contacts no host
# and has no identity to borrow. In particular it performs no build — the
# contract describes builds, and running one here would be minutes of work for an
# answer the document already states.
permissions:
  contents: read

jobs:
  # ── the Go consumer ───────────────────────────────────────────────────────
  go-contract:
    runs-on: ubuntu-latest
    # Seconds of real work behind a toolchain setup and a package build. The one
    # deliberate cost inside it is the readiness case that lets the frozen
    # two-second database bound actually elapse. Finite and chosen, so a wedged
    # run turns the board red in minutes instead of holding a slot for GitHub's
    # 6-hour default.
    timeout-minutes: 10
    steps:
      # ── PROBE-ONLY, NEVER MERGE — delete with the probe ──────────────────
      # Semantic under test: what a reusable workflow OBSERVES when it is
      # called. A called run does not report a `workflow_call` event; it reports
      # the CALLER's `event_name` and `ref`. This step is the evidence for that
      # claim rather than an assumption about it, so `test` — not a print — is
      # what decides: a called run that does not see `pull_request` fails the
      # lane and the verdict job reports it.
      #
      # It is placed before `checkout` because it reads nothing from the tree,
      # and it is skipped entirely on `push: main`, where `inputs` is empty and
      # the `if` reads as null. It changes no existing step, command or bound.
      - name: 'PROBE (never merge): the called run observes the caller event'
        if: ${{ inputs.probe_assert_pull_request }}
        env:
          PROBE_EVENT_NAME: ${{ github.event_name }}
          PROBE_REF: ${{ github.ref }}
          PROBE_CALLER_WORKFLOW: ${{ github.workflow }}
        run: |
          printf 'called lane=ops-deploy-contract event_name=%s ref=%s github.workflow=%s\n' \
            "$PROBE_EVENT_NAME" "$PROBE_REF" "$PROBE_CALLER_WORKFLOW"
          test "$PROBE_EVENT_NAME" = 'pull_request'
      # Third-party actions are pinned to a full commit SHA (with the
      # human-readable version in a trailing comment) so a compromised or
      # retagged upstream release can't silently change what runs here.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: server/go.mod
      # One package, one named prefix of test functions, no `-race`. The race
      # lane is `go.yml`'s eight shards and is not what a document edit needs;
      # `-count=1` is what stops a cached PASS from answering for bytes that
      # changed.
      - name: The Go half of the ops deploy contract
        working-directory: server
        run: go test ./ -run '^TestOpsDeployContract' -count=1
