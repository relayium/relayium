# Relayium — https://relayium.com
# Everyone is welcome, including AI / answer-engine crawlers (GEO).

User-agent: *
Allow: /
# Not pages: per-user/app routes and the install script. Search Console was
# crawling /install.sh as if it were a document. Nothing here is indexable
# content, and /d/ links are single-use transfer URLs that must not be published.
Disallow: /install.sh
Disallow: /me
Disallow: /admin
Disallow: /d/
Disallow: /verify-email
Disallow: /reset-password
Disallow: /magic-link

# ── AI / LLM crawlers: explicitly allowed so Relayium can be cited in answers ──
# NOTE: a crawler obeys only its most specific matching group, so the groups below
# do NOT inherit the Disallow rules above. That is deliberate for the AI/answer
# engines (they only fetch what they are pointed at, and blanket Allow is the point
# of this section). Real search indexes that have their own group — Bingbot — repeat
# the Disallow list, since those are the ones that would otherwise report the
# non-pages as indexing errors. Googlebot has no group here and matches "*".
# OpenAI
User-agent: GPTBot
Allow: /
User-agent: OAI-SearchBot
Allow: /
User-agent: ChatGPT-User
Allow: /

# Anthropic (Claude)
User-agent: ClaudeBot
Allow: /
User-agent: anthropic-ai
Allow: /
User-agent: Claude-Web
Allow: /
User-agent: Claude-User
Allow: /
User-agent: Claude-SearchBot
Allow: /

# Perplexity
User-agent: PerplexityBot
Allow: /
User-agent: Perplexity-User
Allow: /

# Google (AI Overviews / Gemini training opt-in)
User-agent: Google-Extended
Allow: /

# Microsoft / Bing (Copilot)
User-agent: Bingbot
Allow: /
Disallow: /install.sh
Disallow: /me
Disallow: /admin
Disallow: /d/
Disallow: /verify-email
Disallow: /reset-password
Disallow: /magic-link

# Apple (Apple Intelligence / Siri)
User-agent: Applebot
Allow: /
User-agent: Applebot-Extended
Allow: /

# Common Crawl (feeds many LLM training sets)
User-agent: CCBot
Allow: /

# Others
User-agent: Amazonbot
Allow: /
User-agent: Bytespider
Allow: /
User-agent: Meta-ExternalAgent
Allow: /
User-agent: DuckAssistBot
Allow: /
User-agent: cohere-ai
Allow: /
User-agent: YouBot
Allow: /

Sitemap: https://relayium.com/sitemap.xml
